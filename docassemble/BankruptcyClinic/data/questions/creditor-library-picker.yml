# ──────────────────────────────────────────────
#  Creditor Library Picker
# ──────────────────────────────────────────────
#  Include this file to offer users a chance to pre-populate creditors
#  from the clinic's shared library before the manual gather() flow.
#
#  Sets:
#    - creditor_library_picker_done (True when the picker step is complete)
#
#  The picker pre-fills name/address into the relevant DAList so the
#  user only has to fill the debt-specific details when the gather()
#  questions appear.
# ──────────────────────────────────────────────
---
# Seed the clinic's default common creditors (IRS, Nebraska Dept of Revenue)
# before the picker renders so they appear in the list. Idempotent by name.
code: |
  seed_default_creditors()
  creditor_library_seeded = True
---
# The picker screen — shown once before the creditor-gather sections.
# It offers checkboxes for every creditor in the library and lets
# the user pick which ones to pre-load.
id: creditor_library_picker
question: |
  Common Creditors
subquestion: |
  % if len(get_creditor_choices()) > 0:
  Your clinic maintains a list of commonly seen creditors.
  **Only select a creditor if you actually owe it money.** Each one you select
  is added to your creditor schedules (Schedule E or F) with its name and
  address pre-filled, and you will be asked for the amount you owe it.

  Selecting a creditor here is **not** needed just to send it a notice — leave
  it unchecked if you don't owe it anything. You can always add more creditors
  manually in the next steps.
  % else:
  No common creditors have been added to the library yet.
  Click **Continue** to enter your creditors manually.
  % endif
fields:
  - Select creditors to pre-fill: library_selected_ids
    datatype: checkboxes
    code: |
      [{c['value']: c['label']} for c in get_creditor_choices()]
    required: False
    show if:
      code: |
        len(get_creditor_choices()) > 0
continue button field: creditor_library_picker_done
---
# Code block that runs after the picker to inject selected creditors
# into the unsecured nonpriority claims list (the most common case).
# Secured creditors need too many additional fields to pre-fill usefully,
# but nonpriority unsecured creditors share the same name/address structure.
code: |
  _all_records = get_all_creditors()
  _injected_count = 0

  # Idempotency guard: if this block is interrupted mid-run by a seek and
  # re-executed, a creditor already appended must not be appended again
  # (duplicate IRS/NDR rows the filer then has to delete — Roxanne UAT).
  _already_present = set()
  for _lst in (prop.priority_claims, prop.nonpriority_claims):
    for _c in getattr(_lst, 'elements', []):
      _nm = str(getattr(_c, 'name', '') or '').strip().upper()
      if _nm:
        _already_present.add(_nm)

  if defined('library_selected_ids'):
    for _sel_id in library_selected_ids.true_values():
      _rec_id = int(_sel_id)
      if _rec_id in _all_records:
        _data = _all_records[_rec_id]
        if str(_data.get('name', '') or '').strip().upper() in _already_present:
          continue
        _ctype = _data.get('type', '')
        if _ctype in PRIORITY_CREDITOR_TYPES:
          # Priority claim (Schedule E) — e.g. tax debt (IRS / NE Dept of
          # Revenue), domestic support. Route here, not to nonpriority.
          # appendObject() returns the new item and does NOT trigger
          # gathering. len() on the ungathered list DID — selecting a
          # priority-type library creditor (IRS / NE DoR) fired the
          # "any PRIORITY unsecured debts?" gate right here, BEFORE secured
          # creditors (Roxanne, June 4), instead of in the 106EF section.
          _claim = prop.priority_claims.appendObject()
          _claim.type = _ctype
          prop.priority_claims.there_are_any = True
        else:
          # Nonpriority claim (Schedule F)
          _claim = prop.nonpriority_claims.appendObject()  # no len(): see above
          if _ctype in [
            'Student loans',
            'Medical',
            'Credit Card',
            'Contract',
            'Other',
            'Obligations arising out of a separation agreement or divorce that you did not report as priority claims',
            'Debts to pension or profit-sharing plans, and other similar debts',
          ]:
            _claim.type = _ctype
          else:
            _claim.type = 'Other'
            _claim.other_type = _ctype
        # Common pre-fill (name/address/who) for either list
        _claim.name = _data['name']
        _claim.street = _data.get('street', '')
        _claim.city = _data.get('city', '')
        # The library stores states as 2-letter abbreviations ('NE'/'PA') but the
        # claim-detail dropdown uses full names (get_all_us_states()). Convert so
        # the value matches an option — otherwise the dropdown shows blank and the
        # user is forced to re-select the state (Roxanne feedback).
        _claim.state = state_name(_data.get('state', ''))
        _claim.zip = _data.get('zip', '')
        _claim.who = 'Debtor 1 only'
        _claim.account_number = _data.get('account_suffix', '')
        _injected_count += 1

  creditor_library_injected = _injected_count
